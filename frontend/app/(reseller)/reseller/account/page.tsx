'use client';

import { AccountSettings } from '@/components/account-settings';

/** Password and login phone. The shell sends anyone holding a temporary password here. */
export default function AccountPage() {
  return <AccountSettings />;
}
